package Graphs;

public class Dangling {


	
	/*public void dfsTraversal(Node node) {
		Stack<Node> stack = new Stack<Node>();
		stack.add(node);
		node.visited = true;
		
		while (!stack.isEmpty()) {
			Node element = stack.pop();
			System.out.println(element.getValue());

			ArrayList<Node> neighbours = new ArrayList<Node>(element.getAdjacency());
			for (int i = 0; i < neighbours.size(); i++) {
				Node n = neighbours.get(i);
				if (n != null && !n.visited) {
					stack.add(n);
					n.visited = true;

				}
			}
		}
	}*/
	
	
	

	
	
}
